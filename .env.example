# openwebui-wp-poc/.env
# ======= DOMAIN & EMAIL =======
# Your base domain (no protocol). Example: example.com
DOMAIN=agnsai.giize.com
# Email to use for Let's Encrypt SSL (you'll set this in NPM UI too)
LE_EMAIL=asecginli@gmai.com

# Map subdomains for each service (change if you like)
CMS_DOMAIN=cms.${DOMAIN}
API_DOMAIN=api.${DOMAIN}
CHAT_DOMAIN=chat.${DOMAIN}
OWUI_ORIGIN=https://${CHAT_DOMAIN}

# ======= WORDPRESS / DATABASE =======
PORT=3000
WP_DB_NAME=wordpress
WP_DB_USER=wpuser
WP_DB_PASSWORD=...password...
WP_DB_ROOT_PASSWORD=...password...
# ionos-vm-poc/bridge-api/.env
# ======= Bridge API (to connect OpenWebUI with WordPress) =======
# Base URL of your WordPress site (no trailing slash)
API_BASE_URL=https://${API_DOMAIN}
WP_BASE_URL=https://${CMS_DOMAIN}
# For Application Creadintials (Settings > Application Passwords):
WP_USER=admin
WP_APP_PASSWORD=...sample_password... oCOO yV15 qGnd hP5S Q1Ud H4qx

# ======= OpenWebUI (Model Provider, optional if you set via UI) =======
# If IONOS AI Model Hub is OpenAI-compatible, set your base URL + key here.
# Otherwise leave blank and configure within OpenWebUI Admin > Settings.
OPENWEBUI_API_BASE_URL=https://openai.inference.de-txl.ionos.com/v1
OPENWEBUI_API_KEY=...sample_key....... eY.dsfjlkjldsfjlsjf......jflksjflksjflksjflksjflksjflkjs
BRIDGE_API_KEY=...sample_key....... aBcdkfjlsjf......jflksjflksjflksjflksjflksjflkjs

# ======= Agent Builder (OpenAI Responses / Agents API) =======
# Provide your OpenAI agent or model credentials for messaging integrations.
AGENT_API_KEY=sk-...replace-me...
# Optional: override if you're using Azure OpenAI or self-hosted compatible endpoint
AGENT_API_BASE_URL=https://api.openai.com/v1
# Set either AGENT_ID (preferred for Agent Builder) or AGENT_MODEL (e.g. gpt-4.1-mini)
AGENT_ID=agent_...replace-me...
# AGENT_MODEL=gpt-4.1-mini

# ======= Telegram Integration (optional) =======
# Create a bot with BotFather and paste the token below.
TELEGRAM_BOT_TOKEN=1234567890:abcdefghijklmnopqrstuvwxyz
# Secret token that must be provided via webhook URL query (?secret=) or header.
TELEGRAM_WEBHOOK_SECRET=super-secret-token
# Comma separated list of Telegram chat IDs allowed to interact with the bot (leave blank to allow all).
TELEGRAM_ALLOWED_CHAT_IDS=123456789,987654321
# Set to "true" to expose Telegram via the MCP WebSocket server.
TELEGRAM_MCP_ENABLED=false
# Port for the MCP server (defaults to 3030).
TELEGRAM_MCP_PORT=3030
# Poll interval in milliseconds for getUpdates requests.
TELEGRAM_MCP_POLL_INTERVAL_MS=1000

# ======= WhatsApp Integration (Meta Cloud API, optional) =======
# Used during webhook verification handshake with Meta.
WHATSAPP_VERIFY_TOKEN=choose-a-verify-token
# Long-lived access token from Meta for sending replies.
WHATSAPP_ACCESS_TOKEN=EAAG...replace-me...
# Phone number ID associated with your WhatsApp Business account.
WHATSAPP_PHONE_NUMBER_ID=123456789012345
# API version to target when calling the Cloud API.
WHATSAPP_API_VERSION=v20.0
